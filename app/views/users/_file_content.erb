
  <div id="content">
    <div><%= link_to(image_tag('scrollup.png'),{}, id: 'scrollup')%></div>
    <div class="clear"></div>
    <div class="media">
      <% current_user.resources.each do |resource|%>
      <div class="media-left">
        <%= link_to image_tag(resource.file.url, class: 'media-object', width: '350px', height: '350px'), resource.file.url, target: '_blank' %>
      </div>
        <div class="media-body">
          <h4 class="media-heading"></h4>
        </div>
      <% end %>
    </div>
  </div>
